# ph-civ

`ph-civ` is the public namespace for Predictive History: Civilization.

It currently contains the active public card export, study prompts, literary spines, and validation flow for the Civilization-facing orientation layer.

The Python package still imports as `civ_ph` internally for compatibility, but the public command is `ph-civ`.
