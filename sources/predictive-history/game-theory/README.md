# Game Theory source captures

**gt-01 … gt-29:** legacy import captures consolidated into [`lectures/game-theory/`](../../lectures/game-theory/). Flat `gt-NN.md` files here are **redirect stubs** only (one migration cycle).

Regenerate stubs: `python scripts/consolidate_gt_sources.py`
